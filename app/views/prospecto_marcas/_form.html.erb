<%= form_for(prospecto_marca) do |f| %>
  <% if prospecto_marca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prospecto_marca.errors.count, "error") %> prohibited this prospecto_marca from being saved:</h2>

      <ul>
      <% prospecto_marca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Prospecto_id %>
    <%= f.text_field :Prospecto_id %>
  </div>

  <div class="field">
    <%= f.label :Marca_id %>
    <%= f.text_field :Marca_id %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.check_box :estatus %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
