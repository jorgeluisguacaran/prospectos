<%= form_for(prospecto_meta_corto_plazo) do |f| %>
  <% if prospecto_meta_corto_plazo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prospecto_meta_corto_plazo.errors.count, "error") %> prohibited this prospecto_meta_corto_plazo from being saved:</h2>

      <ul>
      <% prospecto_meta_corto_plazo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :prospecto_id %>
    <%= f.text_field :prospecto_id %>
  </div>

  <div class="field">
    <%= f.label :meta_corta %>
    <%= f.text_field :meta_corta %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.check_box :estatus %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
