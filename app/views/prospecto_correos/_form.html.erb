<%= form_for(prospecto_correo) do |f| %>
  <% if prospecto_correo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prospecto_correo.errors.count, "error") %> prohibited this prospecto_correo from being saved:</h2>

      <ul>
      <% prospecto_correo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Prospecto_id %>
    <%= f.text_field :Prospecto_id %>
  </div>

  <div class="field">
    <%= f.label :correo %>
    <%= f.text_field :correo %>
  </div>

  <div class="field">
    <%= f.label :TipoCorreo_id %>
    <%= f.text_field :TipoCorreo_id %>
  </div>

  <div class="field">
    <%= f.label :estatus %>
    <%= f.check_box :estatus %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
